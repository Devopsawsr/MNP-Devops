pipeline {
    agent {label 'mnp-tech-agent'}
    stages {
        stage ('checkout') {
            steps {
                checkout scm
            }
        }
        stage ('Deploy') {
            steps {
                sh '''
                sudo cp /var/lib/jenkins/workspace/${JOB_NAME}/index.html /var/www/html/
                '''
            }
        }
    }
}
